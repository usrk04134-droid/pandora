# Notice

All files in this directory is auto-generated! Any changes to the files in this directory will be replaced on the next build.

To skip the cleaning and re-population of this directory, run apax build with --ignore-scripts:

```bash
apax build --ignore-scripts
```
